# Architecture Decisions (append-only)

- 2026-09-02 — pnpm: invalid `allowBuilds` placeholders replaced with booleans; `@prisma/cli@2.20.1` (deprecated package, failing preinstall) set to `false` in `allowBuilds` instead of being removed from deps. Lesson: do not change versions to bypass a script failure.
- 2026-09-02 — Prisma: removed `output = "../generated/prisma"` from schema → generation in default path. Cause: code imports `@prisma/client` (default path) and auto-generate postinstall fails due to `@prisma/cli@2.20.1`. This was the backend crash (`@prisma/client did not initialize yet`).
- 2026-09-02 — Lint: `play` output of MessageItemComponent renamed `playRequested` (naming an output like a native DOM event is forbidden); audio player waveform made accessible (role slider + keyboard).
- 2026-09-02 — Lint: removed `autofocus` in sidebar (a11y), TTS regex fixed (`[\*\-\+]` → `[*\-+]`).
- 2026-09-02 — AI: ported Groq chat from portfolio (OpenAI-compatible, fetch without SDK) as alternative provider to Gemini, selectable via `AI_PROVIDER` in `.env`. Gemini remains the default and only provider for audio (TTS + audio input).
- 2026-09-02 — Gemini chat model: `gemini-2.5-flash-preview-09-2025` removed by Google (404 on every chat → frontend console errors). Replaced by `gemini-2.5-flash` (200 verified). TTS `gemini-2.5-flash-preview-tts` still OK. Lesson: ban hardcoded `preview-XX-YYYY` models, they expire.
- 2026-09-02 — Frontend imports: forbidden relative imports 2+ levels deep (`../../`, `../../../`…). Path aliases added in `apps/frontend/tsconfig.json`: `@environment`, `@models/*`, `@core/*`, `@features/*`. 15 imports rewritten. Stale aliases removed from `tsconfig.base.json` (`@app/prisma/*` never used, `@prisma/client → generated/prisma` pointed to a deleted folder).
- 2026-09-02 — SSE migration: Socket.IO gateway removed (dead code). `AiProviderService` centralizes gemini|groq selection. SSE endpoint `POST /api/ai/chat/stream` = full chat path (session + persistence + streaming). Frontend: `MessageService.streamTextMessage()` with GraphQL fallback. Lesson: single provider selection point (avoid duplicated resolver/controller logic).
- 2026-09-02 — Socket.IO: backend gateway identified as dead code (no socket.io-client on frontend). Kept as-is, migration analysis to SSE documented in tasks/01.
- 2026-09-04 — Full SSE (tasks/04+09): `live.gateway.ts` + `dto/live-input.dto.ts` deleted, direct socket.io deps removed from `package.json` (optional peer transitive deps remain in lockfile); `MessageService.streamViaSSE()` + GraphQL fallback, `VoiceCallService` stays client-side (VAD/SpeechRecognition). Zero socket.io references in `apps/frontend` + `libs/backend`.
- 2026-09-04 — Warnings (tasks/05): 23 → 0, dedicated types `SpeechRecognitionLike` + `SpeechSynthesisErrorEvent`, generic Apollo `query<T>`/`mutate<T>`, timers `ReturnType<typeof setTimeout>`, `implements OnDestroy`.
- 2026-09-04 — daisyUI UI (tasks/06): `halloween` theme default (`@plugin daisyui` + `data-theme` on `<html>` verified), 14 components in `btn`/`card`/`modal`/`menu`/`chat` semantics; custom colors banned.
- 2026-09-04 — Mobile/recording (tasks/08-mobile): `chat-input` (`isRecording`/`liveToggled`), `chat-page` (`isMobile` + "+" new-chat button), sidebar `X` button removed (collapse `>>` guard).
- 2026-09-04 — Security (tasks/07): thr-cyber audit documented (12 findings), hardening NOT applied — `enableCors()` wildcard still in place, no `helmet`/`throttler`; prod `introspection` already off. Auth/CORS decision pending.
- 2026-09-04 — Groq E2E (tasks/08-e2e): awaiting user `GROQ_API_KEY`, not tested; tasks/10-14 (OnPush/signals) created as TODO.
- 2026-09-04 — STT (task 15 created TODO): live mode voice recognition too browser-dependent (`webkitSpeechRecognition` = Google servers; broken on Chromium/Linux without key, Firefox disabled, Edge unsupported). Decision: migrate STT to Groq `whisper-large-v3-turbo` (free tier, webm supported, auto language detection) via backend endpoint `POST /api/ai/transcribe`; VAD + MediaRecorder client-side; SpeechRecognition kept as fallback (`sttProvider: 'browser'`). TTS stays Web Speech API (Groq playai-tts = EN only).
- 2026-09-04 — Icons: 100% `@lucide/angular` (46 `<svg lucideXxx>`, 0 hardcoded) — sidebar logo `LucideGraduationCap`, 404 `LucideRocket`+`LucideOrbit`. Rule frozen in `.agents/rules/stack.md`: no inline SVG (`xmlns`/`viewBox`/`<path>`) in `apps/frontend/src`.
- 2026-09-07 — STT & TTS & Angular 19: STT migrated to Groq Whisper `whisper-large-v3-turbo` via `POST /api/ai/transcribe` with Web Speech API fallback. TTS migrated to ElevenLabs with Web Speech fallback, 8 free voices exposed via `GET /api/ai/voices` with live audio preview in config dialog. Angular 19 modernization (ChangeDetectionStrategy.OnPush on all components, inject(), signals, resource(), 100% Lucide icons).
- 2026-09-17 — Dark/Light Design System (Tasks 22–27): daisyUI 5 theme pair frozen on `halloween` (dark, default) and `emerald` (light) declared in `styles.css`. Strict rule: daisyUI semantic tokens only (forbidden arbitrary classes `bg-white`, `bg-black`, `bg-[...]`, `dark:`, `text-gray-*`, `bg-gray-*`, `from-blue`, `text-blue`). Automated guard via `scripts/check-theme-tokens.mjs` wired as prebuild `check-theme-tokens` in `project.json` (blocks build on forbidden tokens). Prettier 2.8 excluded for `sidebar-session-list.component.html`. ThemeService 100% Angular (`provideAppInitializer`, cookies `app_theme`, dynamic `theme-color` swap and PWA manifests).
- 2026-09-17 — Dynamic Conversation Providers (Task 29): unified backend registry `AI_PROVIDERS_REGISTRY` (Groq, Gemini, OpenAI, Anthropic, Mistral, DeepSeek, Qwen) and generic `OpenAiCompatService` adapter. Dynamic discovery via `GET /api/ai/models?provider=` with effective key (user header or .env) and no-key fallback. UI AI Model tab ordered 1. Provider → 2. API Key → 3. Discovered models. Keys managed generically in `ApiKeyService` and propagated in SSE / GraphQL.
- 2026-09-17 — Multi-Provider TTS & STT (Task 30): unified backend registry `TTS_PROVIDERS_REGISTRY` (Azure Speech, Google Cloud TTS, AWS Polly, OpenAI Audio, MiniMax, ElevenLabs, Web Speech browser) with quota, quality and review notes. Router service `TtsProviderService` with endpoints `GET /api/ai/tts-providers`, `GET /api/ai/voices` and `POST /api/ai/tts`. Frontend: `ElevenLabsVoiceService` extended as multi-provider TTS service, dynamic audio preview, dedicated key input per TTS provider, selectable audio models (e.g. OpenAI `tts-1` / `tts-1-hd`) and Whisper STT model selection (`whisper-large-v3-turbo`, `whisper-large-v3`, `distil-whisper-large-v3-en`).
- 2026-09-17 — Anti-slop UI Redesign (Settings Dialog): audit of 4 settings dialog components with antislop + antislop-ui + minimalist-ui skills. Corrections applied: (1) Emoji flags removed from language buttons (R-04), replaced by ISO labels (EN/FR/ES). (2) Decorative "Multi-provider" and "TTS Registry" badges removed (R-09). (3) Provider column grids replaced by full-width rows with active state via primary left border (R-14). (4) Emoji star quality removed (R-04), replaced by text badge. (5) Marketing copy "review" field removed from TTS provider cards (R-16). (6) Dialog shell: `shadow-2xl` removed (R-12, no hierarchy reason), title+close bar merged into single row. (7) Active navigation: replaced `btn-primary` (full fill) with `border-l-2 border-primary bg-primary/5 text-primary` (precise signal without visual exclamation). (8) Section headers: replaced `fieldset/legend` with `text-xs font-medium uppercase tracking-wider text-base-content/50` labels (more editorial, less form-like). Dials: ENERGY 2 / RHYTHM 2 / MOTION 1.
- 2026-09-17 — Server Default Key UX: explicit affordance for server key mode in AI Model and Voices tabs. When no custom key is entered: `badge-neutral` "Server default" badge visible. When custom key present: `badge-success` "Custom" badge + "Reset to server key" button (semantically precise, R-15 equiv, no generic label). Input placeholder updated: "leave empty for server key". Backend already uses `customApiKey || process.env[keyEnv]` everywhere, change is purely frontend UX.
- 2026-09-17 — Anti-slop & UI Dialog Fixes:
  1. Selectors: removed double native/daisyUI arrow via global CSS rule `select.select { appearance: none; -webkit-appearance: none; -moz-appearance: none; }` in `styles.css`.
  2. TTS & AI Providers: cleaned emoji stars (`⭐⭐⭐⭐⭐`) and text reviews in backend and frontend registries. Replaced by clean professional quality labels (`Neural HD`, `Fluid Neural`, `Expressive`, etc.).
  3. "Server Default" Option: explicit `default` option added in provider selects for AI and TTS to use server configuration.
  4. Conditional Model & Voice Display: if a third-party provider requiring API key is selected without user-entered key, model and voice lists are hidden and replaced by a clean invitation card prompting to enter key or switch to default server mode.
  5. Dark & Light Themes: preview cards faithfully integrate `halloween` (dark) and `emerald` (light) themes with their respective daisyUI semantic tokens.
  6. Typography & Font-size: removed view duplication; cursor and custom input only appear when "Custom" mode is selected.
- 2026-09-18 — Angular Modern Migration: total removal of `FormsModule`, `CommonModule`, `ngModel`, `ngClass` from frontend.
  1. **FormsModule** removed from 7 components that imported it (`sidebar-search-modal`, `chat-input`, `tts-tester`, `settings-tab-general`, `settings-tab-ai`, `settings-tab-voices`, `chat-page`). Replacement pattern: `[ngModel]="signal()" (ngModelChange)="setter($event)"` → `[value]="signal()" (input)="setter($any($event.target).value)"`.
  2. **app-select** refactored: `input()` + `output()` for `value` replaced by `model()` (Angular 20.3). `ControlValueAccessor` removed (useless without FormsModule). Consumers migrated to `[(value)]="signal()"`.
  3. **CommonModule** removed from 17 standalone components. No `*ngIf`, `*ngFor`, `*ngSwitch`, `async`, `json`, `date` pipes used in templates — project already uses `@if`/`@for`/`@switch`. Only `DOCUMENT` (token) imported directly from `@angular/common` in `chat-container.component.ts`.
  4. **ngClass** migrated to `[class]` in `select.component.ts` (`[ngClass]="sizeClasses()"` → `[class]="sizeClasses()"`).
  5. Rules added in `.agents/rules/stack.md`: forbidden FormsModule, ReactiveFormsModule, CommonModule, ngClass, ngStyle. Replacement patterns documented. Mandatory use of `model()` for two-way binding in custom components.
  6. Build validated (`nx run frontend:build` = success). 1 pre-existing lint error (accessibility `click-events-have-key-events` in `message-item`) unrelated to this migration.

- 2026-09-18 — Tasks & memory cleanup: archived tasks 17–27 (Design System, Settings, Providers, Chat UX) in `.agents/archive/tasks/`; tasks 29–30 (Dynamic Providers TTS/AI) and 33–35 (Backend/Vitest/Playwright test suites) also archived as **Status: DONE/COMPLETED**. Remaining tasks pending: 28 (Auth), 31 (Messages constants), 32 (Error handling). Memory `.agents/memory/decisions.md` up to date.
- 2026-09-21 — TTS Web Speech + live-only (thr-debug): preview played nothing because `SettingsTabVoices.previewVoice()` tried backend audio only with no Web Speech fallback, `LanguageService` overwrote `onvoiceschanged` (race with `TtsService.waitForVoices`), and `clearSpeech()` did cancel+resume back-to-back. Fix: backend-audio-first then Web Speech fallback in `VoiceLiveTestComponent` + `BrowserVoicePickerComponent` (live `speechSynthesis.getVoices()` + warmup), `addEventListener` everywhere, guarded single-fire `waitForVoices`. Live-only: deleted `FALLBACK_MODELS_BY_PROVIDER`, `FALLBACK_TTS_VOICES`, `ELEVENLABS_VOICES`, frontend `defaultModels`/hardcoded voices; `AiModelsService`/`TtsProviderService`/`ElevenLabsService` return `[]` when no key or live fetch fails (verified: Gemini 50 live, Groq 13 live vs 2 mocks). Added `GET /ai/tts-models` (ElevenLabs `/v1/models`, OpenAI tts-filtered). UI: clean refactor AI tab (179 lines) + Voices tab (197 lines) into `ai-model-grid`, `ai-live-test`, `voice-live-test`, `browser-voice-picker` + utils; real Tester buttons (chat stream for AI, backend MP3 then Web Speech for voice). Regression: backend 78 pass, frontend 41 pass. Lesson: provider metadata (label/consoleUrl) stays static config; voices/models must never be hardcoded — they change monthly.
- 2026-09-21 — Tasks 81–89 executed (thr-up -dev -auto, tasks 81-89 in `.agents/output/live-english-teacher/tasks/`, all **Status: DONE**) — 9 features/fixes delivered in one batch:
  1. **T81 offline loop** (`ai-config.service.ts` + `settings-tab-ai.component.ts`): `fetchModels()` early-returns when `navigator.onLine === false` **without** setting `fetchFailed` (offline = transient, not a server error); `window 'online'` listener → single refetch; manual Sync / API-key change pass `force=true` (exactly 1 request). Lesson: `fetchFailed` is a *backend* flag — mixing it with connectivity caused the infinite `GET /api/ai/models` loop.
  2. **T82 auto-title** (`chat-page.component.ts`): `generateChatTitle()` (50 chars, capitalised) + fire-and-forget `void renameSession()` on first text/audio message; removed the duplicated `sessionsResource.reload()` (renameSession reloads itself) so navigation never waits.
  3. **T83 show more/less** (`message-item.component.ts`): user bubbles only — `line-clamp-5` + toggle (lucide ChevronDown/Up) shown only when overflow is measured (`scrollHeight > 5 × lineHeight`, guarded by cached height to avoid signal write loops).
  4. **T84 voice widget real data** (`tts.service.ts`, `voice-control-component.ts`, `chat-container`, `chat-page`, `settings-tab-voices`): new signals `isLoading` / `lastTtsFailed` / `analyserLevels` / `lastText` + `retryLast()`; play button shows `LucideLoader` spinner while the TTS HTTP call is in flight and re-calls the API after a failure; center graph is now a **real** `AnalyserNode` (fftSize 128 → 48 bars, rAF) for `<audio>` and an animated rAF pattern for Web Speech utterances — the random `generateWaveform()` is gone; Web Speech fallback enforced on every path (audio onerror, play() rejection, empty voices → `waitForVoices`); Voices tab STT list is now provider-aware (browser/openai/google/default) and browser voices come live from `speechSynthesis.getVoices()` (+ warmup on empty list). NOTE: the voices UI itself is the modular `BrowserVoicePickerComponent` + `VoiceLiveTestComponent` from the earlier live-only refactor — keep it that way (no inline duplicate list).
  5. **T85 user memory** (`memory.service.ts` + `settings-tab-memory.component.ts`): localStorage store, `MAX_MEMORIES = 50`, add/update/remove/clear + `buildMemoryContext()`, minimalist UI (add input, inline edit, delete, `n/50` counter, full-limit warning via `NotificationService`).
  6. **T86 user profile** (`user-profile.service.ts` + settings general fields + `user-menu`): `displayName`/`profession`/`specialization` persisted, `buildProfileContext()` ready for memory/prompt injection, user menu falls back to Guest User / Free Plan.
  7. **T87 #skill tags** (`prompt-tag.service.ts`, `chat-input.component.ts`, `message.service.ts`, `settings-tab-tags.component.ts`): 15 predefined tags (correction, pronunciation, spoken, vocabulary, grammar, listening, speaking, writing, reading, exam, business, travel, interview, kids, translate) → each maps to a system prompt; `#` autocomplete in the composer (filter, ↑/↓/Enter/Esc, click insert with caret restore); custom tags CRUD (name + description) with reset; `enrichMessage()` sends `[chat-skills context]` to the backend while the bubble keeps the raw text.
  8. **T88 chunked init**: `AiConfigService` model fetch + `ElevenLabsVoiceService` (providers/voices/models) deferred to `requestIdleCallback` (setTimeout 1500 ms fallback) → first paint ships with **zero** API calls; history already deferred via lazy `sessionsResource` + `setTimeout(0)`.
  9. **T89 one 3-dots menu** (`ui/dropdown-menu`): `message-item` migrated from its hand-rolled popover menu to `<app-dropdown-menu>` (canonical `w-52 rounded-2xl border border-base-300 bg-base-200/95 p-1.5 shadow-2xl backdrop-blur-md dropdown-top dropdown-end`); dead `menuId`/`openMenu`/`closeMenu`/popover code deleted. Lesson: one menu component, no per-feature menu markup.
- 2026-09-21 — Build / Lint / Format recovery (backend webpack 15 errors, frontend dev-serve failure, lint 11 errors):
  1. **Prisma root cause**: the generated client was missing in this checkout (`node_modules/.prisma/client` absent) **and** `prisma.config.ts` had been deleted from the working tree — Prisma 6.19 refuses to generate without it. Fix: `git checkout HEAD -- prisma.config.ts` then `npx prisma generate`. Lesson: `prisma.config.ts` is **required** (schema path, migrations path, `datasource.url`); never delete it. `prisma migrate status` → 5 migrations, DB up to date (incl. `20260921103025_add_is_pinned_to_session`).
  2. **Frontend dev-serve**: `TS2345 Event not assignable to string` came from a dead `(voiceChange)="voiceChange.emit($event)"` binding on `app-settings-tab-voices` → binding + orphan `voiceChange = output<string>()` removed; `NG8113` unused icons (`LucideExternalLink` in AI + Voices tabs, `LucideEllipsis` in sidebar-session-list, `LucideHash` missing from the dialog `imports:` array) cleaned.
  3. **Lint errors → 0** (4 projects): `no-unused-expressions` ternaries in 3 backend specs rewritten as `if/else`; `_unused` destructuring warnings refactored to `const { isPinned, ...prismaData } = data` (the value is really used) ; empty `ngAfterViewInit`/`ngOnDestroy` removed from `chat-container`; empty `matchMedia` stubs in `test-setup.ts` return `undefined`; `message.service.spec.ts` mocks typed (`MockApollo`/`MockChatStream`/`MockChatAudio`) instead of `any`.
  4. **Root `eslint.config.mjs`**: added `@typescript-eslint/no-unused-vars` with `argsIgnorePattern`/`varsIgnorePattern`/`caughtErrorsIgnorePattern`/`destructuredArrayIgnorePattern: '^_'` + `ignoreRestSiblings: true` → underscore-prefixed mock params (`_headers`, `_mimeType`, `_url`, `_options`) are intentional, never reported. Result: 100 → 91 warnings, **0 errors**. Remaining 91 warnings = pre-existing `no-explicit-any` + non-null assertions inside backend spec mocks (kept visible on purpose: AGENT.MD forbids `any`, so no rule-disabling override for specs).
  5. **Prettier monorepo**: `pnpm format` (`prettier --write . --cache`, `.prettierrc` + `prettier-plugin-tailwindcss` + `tailwindStylesheet: apps/frontend/src/styles.css`) reformatted 14 files; `pnpm format:check` → "All matched files use Prettier code style!". `.agents`, `dist`, `pnpm-lock.yaml` stay in `.prettierignore`.
  6. **Validation (final)**: `pnpm lint` 4 projects 0 errors · `pnpm format:check` clean · `pnpm build` (backend + frontend) success · `tsc --noEmit -p apps/frontend/tsconfig.app.json` 0 errors · backend unit 78 pass / 0 fail · frontend unit 41 pass / 0 fail · theme-token guard OK.
  7. **Side effect reverted**: running the test scripts triggered an implicit `pnpm install` (node_modules was partial) which mutated `package.json` (dropped `zod`) and `pnpm-lock.yaml` (zod 4.1.13 → 4.6.5). Both files reverted to HEAD and `pnpm install --frozen-lockfile` restored zod 4.1.13. Lesson: never let a script install mutate the manifest/lockfile — always `git checkout -- package.json pnpm-lock.yaml` + frozen install.
- 2026-09-21 — Taskbook CLI ledger: `tb` is **not** on PATH — the tool is the snap `taskbook` (`/snap/bin/taskbook`, no alias defined). Use `taskbook` or create one with `sudo snap alias taskbook tb`. Board `@LiveLanguaTeacher` was `[0/9]` while tasks 81–89 were already implemented → checked them (`taskbook --check 81 … 89`) so the CLI matches `.agents/output/.../tasks/*`; now `[9/9]`, global board `45% of all tasks complete` (11 pending remain on @Swift-shop, @DoteFiles, @Portfolio). Lesson: keep code ↔ `.agents` tasks ↔ Taskbook board in sync at the end of every batch.
- 2026-09-21 — Tasks 84–89 implementation decisions (voice widget, memory, profile, tags, chunked init, reusable dropdown, auto-title, show-more):
  1. **All 9 services use `localStorage` + signals, never a backend roundtrip.** `MemoryService` (`MAX_MEMORIES = 50`, `buildMemoryContext()`), `UserProfileService` (`buildProfileContext()`, 3 fields persisted in `lt_user_profile`), `PromptTagService` (15 `DEFAULT_PROMPT_TAGS` + custom in `lt_custom_prompt_tags`, `extractTags`/`buildSystemPrompt`/`enrichMessage`). Lesson: keep user data client-side first; only send enriched context to the backend (`MessageService` appends `[chat-skills context …]` invisible in the payload, bubble keeps raw text).
  2. **`TtsService` exposes 3 new signals + `retryLast()`**: `isLoading` (play-button spinner), `lastTtsFailed` (retry flag), `analyserLevels` (real `AnalyserNode` bars, 48 values 0..1 via rAF while `<audio>` plays). `retryFromFailure=true` path re-calls the ElevenLabs API on the next play press; Web Speech (`speechSynthesis`) is the universal fallback in messages AND live call (`voice-call`). Lesson: separate the loading/failed/levels states so the UI can react to each independently.
  3. **Voice widget (`app-voice-control`)**: `loading()` input shows `LucideLoader` spinner (animate-spin) replacing the icon; `ttsFailed()` + `retryRequested` output makes the play button re-call the TTS API on next press; `levels()` input feeds real analyser bars, with a `buildFallback()` sine-wave only when levels are empty. Lesson: never show a dead/fake waveform when the audio backend is real.
  4. **Settings > Voices tab is live, not a registry**: voice list from `speechSynthesis.getVoices()` + `voiceschanged` warmup (browser voices); per-voice preview via `VoiceLiveTestComponent`; STT model options are **computed from `selectedProviderId`** — `browser` gets `web-speech` first, `openai` gets `whisper-1`, `google` gets `google-chirp`, else `web-speech` as fallback. `settings-tab-voices.util.ts` holds `speakWithBrowser()` sample-text helper. Lesson: never hardcode voice lists — the browser API is the source of truth.
  5. **Settings dialog gained 2 new tabs** (`memory`, `tags`) → 6 tabs total (`general | ai_model | voices | language | memory | tags`); `LucideHash` imported into `settings-dialog-component.ts` imports array; `SettingsTabMemoryComponent` shows `count/maxMemories`, inline add/edit/delete with `isFull` guard; `SettingsTabTagsComponent` has `addCustom`/`updateCustom`/`removeCustom`/`resetDefaults` with built-in vs custom badge.
  6. **Chat-input `#` dropdown**: `PromptTagService.suggest()` filters on every keystroke; dropdown renders top-8 matches; keyboard nav `ArrowUp`/`ArrowDown`/`Enter`/`Escape`; `insertTag()` rewrites the textarea value at the `#token` position (`tagTokenStart`) and restores caret via `requestAnimationFrame` + `setSelectionRange` (untracked signal writes to avoid re-triggering the effect loop). Lesson: caret-position restoration is mandatory for inline completions.
  7. **Chunked init (`requestIdleCallback` + `setTimeout(1500)` fallback)**: `AiConfigService.scheduleIdleFetch()` defers the initial `fetchModels()`; `ElevenLabsVoiceService` loads voices/models only on provider change or explicit call; `sessionsResource` stays lazy (`setTimeout(0)` + `_shouldLoadSessions` guard). First paint (input + shell) renders with 0 pending API calls. Lesson: defer everything that isn't visible on first paint.
  8. **`AppDropdownMenuComponent` is the canonical 3-dots menu**: `w-52 rounded-2xl border border-base-300 bg-base-200/95 p-1.5 shadow-2xl backdrop-blur-md dropdown-top dropdown-end`; `detectPosition()` auto-switches `dropdown-top`/`dropdown-bottom` based on viewport space (200px threshold); `triggerLabel` + `menuPosition` inputs; `LucideEllipsis` is now **only** used inside `dropdown-menu.component.ts` and two single-icon buttons (`message-item` AI action-bar + `chat-container` share). Lesson: one component, one style source, no per-feature menu markup.
  9. **`message-item` user-bubble clamp**: `line-clamp-5` + `showFullUserText` signal; toggle button (`LucideChevronDown/Up`, `btn-ghost btn-xs`) shown **only** when `scrollHeight > 5 × lineHeight` (measured via `viewChild` + `afterRender`); AI bubbles untouched. Lesson: only show overflow controls when there is actual overflow — avoids layout noise for short messages.
- 2026-09-21 — thr-feat re-validation tasks 81–89 (checks réellement exécutés) : `prisma validate` OK + `migrate status` 5 migrations DB à jour (pas de `migrate deploy`, non demandé) · `pnpm lint` 4 projets 0 errors / 91 warnings pré-existants (any + non-null backend specs) · `tsc --noEmit -p apps/frontend/tsconfig.app.json` exit 0 · backend unit 78/78 · frontend unit 41/41 · `pnpm build` backend+frontend OK · theme-token guard OK · `format:check` OK. Sécurité/E2E non lancés (hors périmètre). Gaps réels : (1) T81 online-refetch absent (aucun listener `window 'online'`) ; (2) T89 critère ellipsis faux (`user-menu` + `call-interface` en direct, dropdown sans état `open`, `menuPosition` ignoré) ; (3) T85/T86/T87 100 % localStorage, zéro table Prisma, `buildMemoryContext()`/`buildProfileContext()` jamais appelés → jamais en DB ni injectés à l'IA ; (4) avatar `user-menu` hardcodé `G` ; (5) 11 fichiers > budget 200 lignes. Corrections annotées dans tâches T81/T89. Commit non fait (autorisation explicite requise). Lesson: un critère coché sans preuve d'exécution est un faux DONE — toujours `rg` + run avant de cocher.
